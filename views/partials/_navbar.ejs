<nav>
    <a href="/">Home</a>
    <% if(user) { %>
    <a href="/users/<%=user._id%>/foods">View my Pantry</a>
    <a href="/auth/sign-out">Sign Out</a>
    <% 
    <!-- console.log(locals) -->
    if(locals.currentUrl !== "/users"){ %><a href="/users">Explore Our Community</a> <%}%>
    <%if(locals.currentUrl !== "/recipes"){ %><a href="/recipes">my recipes</a> <%}%>
    <%if(locals.currentUrl !== "/recipes/new"){ %><a href="/recipes/new">Add New Recipe</a> <%}%>


    <% } else { %>
    <a href="/auth/sign-in">Sign In</a>
    <a href="/auth/sign-up">Sign Up</a>
    <% } %>
  </nav>
  